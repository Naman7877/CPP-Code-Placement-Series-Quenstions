int MissingNumber(vector<int>& array, int n) {
        // Your code goes here
         int sum=0;
        for(int i=0;i<n-1;i++){
            sum=sum+array[i];
        }
        
        // this is formula for find the sum of first n numbers
        return (n*(n+1))/2-sum;
    }
};
